import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    common: {
      black: "#040404",
    },
    primary: { main: "#f23424" },
    secondary: { main: "#ffc527" },
  },
});
export default theme;
